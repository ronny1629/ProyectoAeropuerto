@model IEnumerable<SistemaAeropuerto.hangarr>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ubicacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.capacidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ubicacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.capacidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_hangar }, new { @class = "btn btn-warning glyphicon glyphicon-edit" }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_hangar }, new { @class = "btn btn-info" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_hangar }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
